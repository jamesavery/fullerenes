if(GTest_FOUND)
  add_executable(test_csr test_csr.cc)
  target_link_libraries(test_csr fullerenes GTest::gtest_main)
  gtest_discover_tests(test_csr)
else()
  message(STATUS "GTest not found -- skipping test_csr in playground/spanify/")
endif()
